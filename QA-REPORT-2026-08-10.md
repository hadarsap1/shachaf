# דו"ח QA לקראת גל המשתמשים — 10.8.2026

**מה נבדק:** `claude/e2e-qa-traffic-surge-s8uhpu` (מבוסס `main` @ 1f496d2)
**איך:** חבילת E2E חדשה שמריצה את האפליקציה האמיתית ב-Chromium מול אמולטורים
של Firebase (Auth + Firestore), בנוסף לבדיקות היחידה, חוקי ה-Firestore, ה-lint
והבילד. ההרצה כולה מקומית — לא נגענו בפרויקט הפרודקשן ולא בנתוני אמת.

---

## שורה תחתונה

המערכת מוכנה לגל, **אחרי מיזוג הענף הזה**. נמצא באג אחד חמור שהיה משבית את
לוח המשימות לכל משפחה שתיכנס מחר — הוא תוקן ומאומת. שאר הזרימות עברו נקי.

| בדיקה | תוצאה |
|---|---|
| בדיקות יחידה (`npm test`) | ✅ 154/154 |
| חוקי Firestore (`npm run test:rules`) | ✅ 75/75 (70 קודמות + 5 חדשות) |
| E2E בדפדפן (`npm run test:e2e`) | ✅ 52/52 |
| בילד פרודקשן (`npm run build`) | ✅ עובר |
| Lint | ללא שינוי מהבסיס (15 שגיאות, 42 אזהרות — כולן קיימות מקודם) |

---

## 🔴 P0 — לוח המשימות של המשפחות היה שבור לגמרי (תוקן)

**מה קרה:** כל משפחה שנכנסה ל"משימות" קיבלה מסך ריק — בלי משימות **ובלי
טפסים למילוי**. בקונסול: `FirebaseError: Property assignedTo is undefined`.

**למה:** אי-התאמה בין מודל הנתונים לחוקי האבטחה.

- מסך הניהול יוצר משימות כ**שידור** — `targetGroups: ['all' | תפקיד | כיתה]`,
  בלי שדה `assignedTo` בכלל (`AdminTasksPage.jsx:53`).
- חוק ה-Firestore דרש `resource.data.assignedTo == request.auth.uid`.
  שאילתת רשימה בלי פילטר לא יכולה להוכיח את זה מראש → **כל השאילתה נדחית**.
- `TasksPage.jsx` קורא ל-`getTasks()` בלי פרמטר → נדחה תמיד למי שאינו מנהל.
- ה-`Promise.all` שם הפיל גם את הטפסים, הילדים וההגשות יחד עם המשימות.

הוכחה מול האמולטור (לפני התיקון):

```
— TasksPage עושה: getTasks() → רשימה לא מסוננת של /tasks
   DENIED
— הדשבורד עושה: getTasks(uid) → where assignedTo == uid
   ALLOWED, 0 מסמכים  (משימות שידור פשוט לא נראות שם)
— משפחה מסמנת משימה כבוצעה
   DENIED
```

**התיקון:**
1. `firestore.rules` — קריאת `/tasks` פתוחה לכל משתמש מחובר. מסמך משימה מכיל
   תוכן בית-ספרי בלבד (כותרת, תיאור, תאריך יעד, קהל יעד) — אין בו מידע על
   משפחות. כתיבה נשארה למנהלים בלבד.
2. `TasksPage.jsx` — `Promise.allSettled` במקום `Promise.all`, כך ששאילתה
   אחת שנופלת לא מוחקת את כל הדף, ומוצגת הודעה במקום מסך ריק שקט.
3. חמש בדיקות חוקים חדשות שנועלות את ההתנהגות (משפחה קוראת ✓, כותבת ✗).

**⚠️ החלטה שנשארה פתוחה — סימון משימה כבוצעה.**
היום `status` הוא שדה יחיד על מסמך משותף, וכתיבה מותרת למנהלים בלבד. כלומר
משפחה שתלחץ על משימה — הסימון יקפוץ חזרה. שתי אפשרויות:
- **מומלץ:** להוסיף `doneBy: [uid]` למסמך, לאפשר בחוקים לעדכן רק את ה-uid
  של עצמך, ולגזור את הסטטוס לכל משפחה בנפרד. זו גם ההתנהגות הנכונה —
  אחרת משפחה אחת שמסמנת "בוצע" מסמנת עבור כולם.
- לחלופין להסתיר את הטוגל למי שאינו מנהל, ולהשאיר את הלוח לקריאה בלבד.

לא נגעתי בזה — זו החלטת מוצר, ולא רציתי לשנות סמנטיקה של פיצ'ר בלילה שלפני.
בכל מקרה זה עדיף על המצב הקודם, שבו הדף היה ריק לחלוטין.

---

## 🟠 P1 — ניטור השגיאות היה חסום ב-CSP (תוקן)

`connect-src` ב-`vercel.json` לא כלל את Sentry. אם `VITE_SENTRY_DSN` מוגדר
בפרודקשן, הדפדפן חוסם כל דיווח שגיאה — כלומר דווקא ביום העמוס ביותר לא היו
מגיעים דיווחים. הוספתי `https://*.sentry.io`.

**כדאי לוודא היום:** ש-`VITE_SENTRY_DSN` באמת מוגדר ב-Vercel. בלי זה אין
ניטור בכלל (הקוד עושה no-op בשקט).

---

## 🟡 P2 — דברים שכדאי לדעת, לא חוסמים את מחר

**1. צ'אט ועדות/קבוצות מוגבל ל-200 הודעות בלי מיון.**
`subscribeCommitteeChat` / `subscribeGroupChat` משתמשים ב-`limit(200)` בלי
`orderBy`. Firestore מחזיר במקרה כזה 200 מסמכים לפי סדר ה-ID (אקראי), לא
את האחרונים. עד 200 הודעות בצ'אט הכול תקין; מעבר לזה המשתמשים יראו תת-קבוצה
שרירותית. עם צ'אט פעיל אפשר להגיע לזה תוך שבועות.
*התיקון דורש אינדקס מורכב* (`committeeId ==` + `createdAt desc`), ובריפו אין
`firestore.indexes.json` ו-ה-workflow מפרסם רק חוקים. לכן: **לא לפני מחר** —
אבל שווה לתזמן.

**2. גופנים חיצוניים בנתיב הקריטי.**
`index.css` מייבא Heebo/Inter מ-Google Fonts ב-`@import` — כלומר הצביעה
הראשונה מחכה לצד שלישי. הוספתי `preconnect` ל-`index.html` (חוסך DNS+TLS).
אם רוצים חסינות מלאה, אירוח עצמי של הגופנים מוציא את התלות הזו לגמרי.

**3. `api/chat.js` — מגבלת הקצב לדקה היא per-instance.**
`rateLimitMap` יושב בזיכרון של פונקציית Vercel. בגל תנועה יש כמה מופעים
במקביל, כך שהתקרה בפועל גבוהה מ-5/דקה. המגבלה היומית (30) נשמרת ב-Firestore
ולכן אמיתית — היא השומר האפקטיבי. לא דחוף, רק שתדע.

**4. באנדל ראשי 664KB (176KB gzip).**
דפי האדמין כבר lazy. ה-PWA מקאשת אחרי הביקור הראשון, ולכן זה בעיקר משנה
לכניסה ראשונה במובייל סלולרי — בדיוק מה שיקרה מחר. לא הייתי נוגע בזה עכשיו.

**5. שדה `order` הוא תנאי להופעה.**
ועדות וקבוצות נקראות עם `orderBy('order')`, ו-Firestore **משמיט בשקט** מסמך
בלי השדה הזה. שני מסלולי היצירה באפליקציה מגדירים `order`, אז הכול תקין —
אבל ועדה שתיווצר ידנית ב-Firebase Console בלי `order` פשוט לא תופיע לאף אחד.
(נפלתי על זה בעצמי כשזרעתי נתוני בדיקה.)

---

## ✅ מה נבדק ועובד — E2E מלא בדפדפן

| תרחיש | דסקטופ | מובייל |
|---|---|---|
| מסך כניסה, מיתוג שחף+, ללא שגיאות קונסול | ✅ | ✅ |
| שלושת הדפים המשפטיים נגישים בלי התחברות | ✅ | — |
| נתיב לא מוכר ומסך מוגן מפנים ל-`/login` | ✅ | — |
| **הרשמה חדשה** עם מייל וסיסמה | ✅ | — |
| שער ההסכמה חוסם תוכן — אין אירועים/משימות לפני אישור | ✅ | — |
| אישור תקנון פותח את הדשבורד; רענון לא מבקש שוב | ✅ | — |
| **משפחה שהוזמנה מייבוא** — לא מגיעה לתוכן הקהילה | ✅ | — |
| ניווט ישיר ל-`/events` לא עוקף את השער | ✅ | — |
| חשבון בסטטוס `pending` לא רואה תוכן קהילתי | ✅ | — |
| דשבורד: אירוע קרוב + הכיתה של הילד | ✅ | ✅ |
| **לוח משימות מציג משימת שידור** (הבאג שתוקן) | ✅ | — |
| אירועים: אירוע עתידי מוצג, אירוע שחלף לא | ✅ | ✅ |
| **אישור הגעה נשמר ושורד רענון** | ✅ | — |
| בקשת הצטרפות לוועדה — כולל צ'קבוקס ההסכמה — נרשמת ומופיעה מיד | ✅ | — |
| 11 דפי משפחה נטענים ללא שגיאות קונסול | ✅ | ✅ |
| 13 מסכי ניהול נטענים ללא שגיאות קונסול | ✅ | — |
| רשימת החברים מציגה את מי שנרשם | ✅ | — |
| אין גלישה אופקית באף מסך (390×844) | — | ✅ |
| התנתקות מחזירה למסך הכניסה | ✅ | — |

בכל צעד נאספו שגיאות קונסול, שגיאות JS ובקשות same-origin שנכשלו, ובנפרד
נאספו שגיאות הרשאה מ-Firestore — שם צף באג לוח המשימות.

---

## מה נוסף לריפו

- `tests/e2e/` — החבילה עצמה (`seed.mjs`, `run.mjs`, `README.md`).
  הרצה: `npm run test:e2e` (דורש Java ו-`npm i -D playwright`).
  Playwright **לא** נוסף ל-`package.json` בכוונה — כדי שלא יורידו דפדפן
  בכל בילד של Vercel.
- `src/lib/firebase.js` — חיבור לאמולטורים מאחורי `VITE_USE_EMULATORS=1`,
  עטוף ב-`import.meta.env.DEV` כך שבילד פרודקשן לא יכול להצביע ל-localhost.
- `firebase.json` — פורטים לאמולטורים.

## מה מומלץ לעשות היום

1. למזג את הענף ל-`main` (מפרסם גם את חוקי ה-Firestore המתוקנים).
2. לוודא אחרי הפריסה: להיכנס כהורה אמיתי ולפתוח "משימות" — צריכות להופיע.
3. לוודא ש-`VITE_SENTRY_DSN` מוגדר ב-Vercel.
4. להחליט לגבי סימון משימה כבוצעה (ראו P0 למעלה).
